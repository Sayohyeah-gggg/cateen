<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xawl.cateen.mapper.ForumLikeMapper">

    <resultMap id="LikeVOMap" type="com.xawl.cateen.vo.ForumLikeVO">
        <id column="id" property="id"/>
        <result column="post_id" property="postId"/>
        <result column="user_id" property="userId"/>
        <result column="created_at" property="createdAt"/>
        <result column="nickname" property="userNickname"/>
        <result column="avatar" property="userAvatar"/>
    </resultMap>

    <select id="selectLikePage" resultMap="LikeVOMap">
        SELECT
            l.id,
            l.post_id,
            l.user_id,
            l.created_at,
            u.nickname,
            u.avatar
        FROM forum_likes l
        LEFT JOIN profiles u ON l.user_id = u.id
        WHERE l.post_id = #{postId}
        ORDER BY l.created_at DESC
    </select>

</mapper>
