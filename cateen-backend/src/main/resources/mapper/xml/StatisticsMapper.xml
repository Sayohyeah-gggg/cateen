<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xawl.cateen.mapper.StatisticsMapper">

    <!-- 统计美食总数 -->
    <select id="countTotalFoods" resultType="java.lang.Long">
        SELECT COUNT(*) FROM foods
    </select>

    <!-- 统计用户总数 -->
    <select id="countTotalUsers" resultType="java.lang.Long">
        SELECT COUNT(*) FROM profiles
    </select>

    <!-- 统计评论总数 -->
    <select id="countTotalComments" resultType="java.lang.Long">
        SELECT COUNT(*) FROM comments
    </select>

    <!-- 统计待审核评论数 -->
    <select id="countPendingComments" resultType="java.lang.Long">
        SELECT COUNT(*) FROM comments WHERE status = 'pending'
    </select>

    <!-- 统计榜单总数 -->
    <select id="countTotalRankings" resultType="java.lang.Long">
        SELECT COUNT(*) FROM rankings
    </select>

    <!-- 计算平均评分 -->
    <select id="calculateAvgRating" resultType="java.math.BigDecimal">
        SELECT IFNULL(AVG(rating), 0) FROM foods WHERE rating > 0
    </select>

    <!-- 统计分类数据 -->
    <select id="statisticsCategoryData" resultType="java.util.HashMap">
        SELECT
            fc.id AS categoryId,
            fc.name AS categoryName,
            COUNT(f.id) AS foodCount,
            IFNULL(SUM(f.rating_count), 0) AS totalComments,
            IFNULL(AVG(f.rating), 0) AS avgRating
        FROM food_categories fc
        LEFT JOIN foods f ON fc.id = f.category_id
        GROUP BY fc.id, fc.name
        ORDER BY foodCount DESC
    </select>

    <!-- 统计用户活动数据 -->
    <select id="statisticsUserActivity" resultType="java.util.HashMap">
        SELECT
            DATE(date_series.date) AS date,
            IFNULL(user_stats.new_users, 0) AS newUsers,
            IFNULL(comment_stats.new_comments, 0) AS newComments,
            IFNULL(food_stats.new_foods, 0) AS newFoods
        FROM (
            SELECT DATE_ADD(#{startDate}, INTERVAL seq DAY) AS date
            FROM (
                SELECT 0 AS seq UNION ALL SELECT 1 UNION ALL SELECT 2 UNION ALL SELECT 3 UNION ALL SELECT 4 
                UNION ALL SELECT 5 UNION ALL SELECT 6 UNION ALL SELECT 7 UNION ALL SELECT 8 UNION ALL SELECT 9
                UNION ALL SELECT 10 UNION ALL SELECT 11 UNION ALL SELECT 12 UNION ALL SELECT 13 UNION ALL SELECT 14
                UNION ALL SELECT 15 UNION ALL SELECT 16 UNION ALL SELECT 17 UNION ALL SELECT 18 UNION ALL SELECT 19
                UNION ALL SELECT 20 UNION ALL SELECT 21 UNION ALL SELECT 22 UNION ALL SELECT 23 UNION ALL SELECT 24
                UNION ALL SELECT 25 UNION ALL SELECT 26 UNION ALL SELECT 27 UNION ALL SELECT 28 UNION ALL SELECT 29
                UNION ALL SELECT 30
            ) AS seq_table
            WHERE DATE_ADD(#{startDate}, INTERVAL seq DAY) &lt;= #{endDate}
        ) AS date_series
        LEFT JOIN (
            SELECT DATE(created_at) AS date, COUNT(*) AS new_users
            FROM profiles
            WHERE DATE(created_at) BETWEEN #{startDate} AND #{endDate}
            GROUP BY DATE(created_at)
        ) AS user_stats ON DATE(date_series.date) = user_stats.date
        LEFT JOIN (
            SELECT DATE(created_at) AS date, COUNT(*) AS new_comments
            FROM comments
            WHERE DATE(created_at) BETWEEN #{startDate} AND #{endDate}
            GROUP BY DATE(created_at)
        ) AS comment_stats ON DATE(date_series.date) = comment_stats.date
        LEFT JOIN (
            SELECT DATE(created_at) AS date, COUNT(*) AS new_foods
            FROM foods
            WHERE DATE(created_at) BETWEEN #{startDate} AND #{endDate}
            GROUP BY DATE(created_at)
        ) AS food_stats ON DATE(date_series.date) = food_stats.date
        ORDER BY date_series.date
    </select>

    <!-- 统计访问数据（暂时返回基于用户活动的模拟数据，实际应接入访问日志表） -->
    <select id="statisticsVisitData" resultType="java.util.HashMap">
        SELECT
            DATE(date_series.date) AS date,
            IFNULL(user_stats.active_users * 5, 0) AS visits,
            IFNULL(user_stats.active_users, 0) AS uniqueVisitors,
            IFNULL(comment_stats.total_comments * 3, 0) AS pageViews,
            25 AS bounceRate
        FROM (
            SELECT DATE_ADD(#{startDate}, INTERVAL seq DAY) AS date
            FROM (
                SELECT 0 AS seq UNION ALL SELECT 1 UNION ALL SELECT 2 UNION ALL SELECT 3 UNION ALL SELECT 4 
                UNION ALL SELECT 5 UNION ALL SELECT 6 UNION ALL SELECT 7 UNION ALL SELECT 8 UNION ALL SELECT 9
                UNION ALL SELECT 10 UNION ALL SELECT 11 UNION ALL SELECT 12 UNION ALL SELECT 13 UNION ALL SELECT 14
                UNION ALL SELECT 15 UNION ALL SELECT 16 UNION ALL SELECT 17 UNION ALL SELECT 18 UNION ALL SELECT 19
                UNION ALL SELECT 20 UNION ALL SELECT 21 UNION ALL SELECT 22 UNION ALL SELECT 23 UNION ALL SELECT 24
                UNION ALL SELECT 25 UNION ALL SELECT 26 UNION ALL SELECT 27 UNION ALL SELECT 28 UNION ALL SELECT 29
                UNION ALL SELECT 30
            ) AS seq_table
            WHERE DATE_ADD(#{startDate}, INTERVAL seq DAY) &lt;= #{endDate}
        ) AS date_series
        LEFT JOIN (
            SELECT DATE(created_at) AS date, COUNT(DISTINCT user_id) AS active_users
            FROM comments
            WHERE DATE(created_at) BETWEEN #{startDate} AND #{endDate}
            GROUP BY DATE(created_at)
        ) AS user_stats ON DATE(date_series.date) = user_stats.date
        LEFT JOIN (
            SELECT DATE(created_at) AS date, COUNT(*) AS total_comments
            FROM comments
            WHERE DATE(created_at) BETWEEN #{startDate} AND #{endDate}
            GROUP BY DATE(created_at)
        ) AS comment_stats ON DATE(date_series.date) = comment_stats.date
        ORDER BY date_series.date
    </select>

</mapper>

