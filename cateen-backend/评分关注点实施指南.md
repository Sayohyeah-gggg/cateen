# 评分关注点功能实施指南

## 📋 已完成的工作

### 1. 数据库设计 ✅
- [x] 创建 `comment_preferences` 表
- [x] 创建 `preference_keywords` 表（NLP关键词）
- [x] 插入测试数据
- [x] 创建统计视图

**SQL文件位置：** `src/main/resources/sql/comment_preferences.sql`

### 2. 后端实体类 ✅
- [x] CommentPreference 实体类
- [x] CommentDTO 添加 `preferences` 字段

### 3. Mapper层 ✅
- [x] CommentPreferenceMapper 接口
- [x] CommentPreferenceMapper.xml SQL映射
- [x] 更新 StatisticsMapper.xml 使用真实数据

### 4. Service层 ✅
- [x] 更新 CommentServiceImpl 支持保存多维度评分
- [x] StatisticsServiceImpl 已支持评分关注点查询

## 🚀 部署步骤

### Step 1: 执行数据库脚本

```bash
# 连接到MySQL数据库
mysql -u root -p

# 执行SQL脚本
source /path/to/cateen-backend/src/main/resources/sql/comment_preferences.sql
```

或者使用Navicat/DataGrip等工具直接执行该SQL文件。

### Step 2: 重启后端服务

```bash
# 如果使用IDEA，直接重启CateenApplication
# 如果使用Maven
mvn spring-boot:run
```

### Step 3: 测试API

#### 测试1：查看评分关注点统计
```bash
GET http://localhost:8080/api/stats/rating-preferences
```

**预期响应：**
```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    {
      "preference": "口味",
      "percentage": 44,
      "color": "#3b82f6"
    },
    {
      "preference": "价格",
      "percentage": 22,
      "color": "#10b981"
    },
    {
      "preference": "环境",
      "percentage": 11,
      "color": "#f59e0b"
    },
    {
      "preference": "服务",
      "percentage": 11,
      "color": "#8b5cf6"
    }
  ]
}
```

#### 测试2：创建带多维度评分的评论
```bash
POST http://localhost:8080/api/comments
Content-Type: application/json
Authorization: Bearer YOUR_TOKEN

{
  "foodId": "food_001",
  "content": "非常好吃，价格实惠，环境优雅，服务周到！",
  "rating": 5,
  "preferences": {
    "taste": 5,
    "price": 5,
    "environment": 4,
    "service": 5
  }
}
```

## 📊 前端集成（后续步骤）

### 当前状态
- ✅ 前端已经调用 `/api/stats/rating-preferences` API
- ✅ 前端页面已经显示评分关注点饼图
- ⏸️ 评论表单还未添加多维度评分UI

### 待实现功能

#### 1. 评论表单添加多维度评分（可选功能）

```typescript
// 前端类型定义
interface CommentPreferences {
  taste?: number;      // 口味 1-5
  price?: number;      // 价格 1-5
  environment?: number;// 环境 1-5
  service?: number;    // 服务 1-5
}

interface CommentForm {
  foodId: string;
  content: string;
  rating: number;
  preferences?: CommentPreferences; // 可选的多维度评分
}
```

#### 2. 评论组件UI示例

```tsx
<div className="space-y-4">
  <h4 className="font-medium">详细评分（可选）</h4>
  
  <div className="grid grid-cols-2 gap-4">
    <div>
      <Label>口味</Label>
      <StarRating 
        value={preferences.taste} 
        onChange={(v) => setPreferences({...preferences, taste: v})} 
      />
    </div>
    
    <div>
      <Label>价格</Label>
      <StarRating 
        value={preferences.price} 
        onChange={(v) => setPreferences({...preferences, price: v})} 
      />
    </div>
    
    <div>
      <Label>环境</Label>
      <StarRating 
        value={preferences.environment} 
        onChange={(v) => setPreferences({...preferences, environment: v})} 
      />
    </div>
    
    <div>
      <Label>服务</Label>
      <StarRating 
        value={preferences.service} 
        onChange={(v) => setPreferences({...preferences, service: v})} 
      />
    </div>
  </div>
</div>
```

## 🔍 验证功能

### 1. 检查数据库表

```sql
-- 查看评论关注点数据
SELECT * FROM comment_preferences;

-- 查看统计结果
SELECT * FROM v_preference_statistics;

-- 查看关键词配置
SELECT * FROM preference_keywords;
```

### 2. 检查API响应

访问评分统计页面，检查饼图是否显示真实数据。

## 📈 未来扩展

### Phase 1: 基础功能（已完成）
- [x] 数据库表设计
- [x] 后端API实现
- [x] 前端数据展示

### Phase 2: 智能分析（待开发）
- [ ] NLP关键词分析评论内容
- [ ] 自动提取关注点
- [ ] 优化分析算法

### Phase 3: 高级功能（待开发）
- [ ] 关注点趋势分析
- [ ] 按分类查看关注点
- [ ] 按食物查看关注点
- [ ] 个性化推荐

### Phase 4: 用户体验优化（待开发）
- [ ] 评论表单添加多维度评分UI
- [ ] 美食详情页显示关注点雷达图
- [ ] 对比不同美食的关注点

## 🎯 当前功能优势

1. **真实数据驱动**：基于数据库真实的评论关注点数据
2. **灵活扩展**：易于添加新的关注点类型
3. **性能优化**：使用视图和索引提高查询效率
4. **兼容性好**：后端API支持多维度评分，但前端可选使用

## ⚠️ 注意事项

1. **数据完整性**：确保先执行SQL脚本再启动后端
2. **权限控制**：评论创建需要登录Token
3. **数据验证**：评分值必须在1-5之间
4. **性能监控**：大数据量时注意查询性能

## 📞 问题排查

### 问题1：API返回空数据
- 检查数据库表是否创建成功
- 检查测试数据是否插入
- 查看后端日志是否有错误

### 问题2：前端饼图不显示
- 检查API是否返回正确数据
- 打开浏览器控制台查看数据
- 检查数据格式是否正确

### 问题3：创建评论失败
- 检查Token是否有效
- 检查请求参数是否完整
- 查看后端日志错误信息

## ✅ 测试清单

- [ ] 数据库表创建成功
- [ ] 测试数据插入成功
- [ ] 后端服务启动无错误
- [ ] GET /api/stats/rating-preferences 返回数据
- [ ] 前端评分统计页面显示饼图
- [ ] 饼图数据与数据库一致
- [ ] 可以创建带多维度评分的评论

## 🎉 完成标志

当以上所有测试清单都打勾后，评分关注点功能就算实施完成了！

