# 美食评估管理系统 - 后端接口文档

## 文档信息

- **项目名称**: 美食评估管理系统
- **文档版本**: v1.0
- **创建日期**: 2025-10-03
- **后端技术栈**: Spring Boot + MyBatis + MySQL
- **接口协议**: RESTful API
- **数据格式**: JSON
- **字符编码**: UTF-8

---

## 1. 接口通用说明

### 1.1 请求头规范

```http
Content-Type: application/json
Authorization: Bearer {token}
```

### 1.2 统一响应格式

#### 成功响应
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    // 具体业务数据
  }
}
```

#### 失败响应
```json
{
  "code": 400,
  "message": "错误描述",
  "data": null
}
```

### 1.3 状态码说明

| 状态码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权/未登录 |
| 403 | 无权限访问 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

### 1.4 分页参数

```json
{
  "pageNum": 1,      // 页码，从1开始
  "pageSize": 20,    // 每页数量
  "total": 100       // 总记录数
}
```

---

## 2. 认证模块 API

### 2.1 用户登录

**接口地址**: `POST /api/auth/login`

**接口描述**: 使用用户名和密码进行身份验证

**请求参数**:
```json
{
  "username": "admin",
  "password": "admin123"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| username | String | 是 | 用户名 |
| password | String | 是 | 密码 |

**响应示例**:
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {
      "id": "1",
      "username": "admin",
      "role": "admin",
      "status": "active"
    }
  }
}
```

---

### 2.2 用户注册

**接口地址**: `POST /api/auth/register`

**接口描述**: 创建新用户账户

**请求参数**:
```json
{
  "username": "testuser",
  "password": "123456",
  "confirmPassword": "123456",
  "phone": "13800138000"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| username | String | 是 | 用户名（4-20字符） |
| password | String | 是 | 密码（6-20字符） |
| confirmPassword | String | 是 | 确认密码 |
| phone | String | 否 | 手机号 |

**响应示例**:
```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "id": "2",
    "username": "testuser",
    "role": "user",
    "status": "active"
  }
}
```

---

### 2.3 获取当前用户信息

**接口地址**: `GET /api/auth/me`

**接口描述**: 获取当前登录用户信息

**请求参数**: 无

**响应示例**:
```json
{
  "code": 200,
  "message": "成功",
  "data": {
    "id": "1",
    "username": "admin",
    "phone": "13800138000",
    "role": "admin",
    "status": "active",
    "createdAt": "2025-09-22T10:00:00Z"
  }
}
```

---

### 2.4 修改密码

**接口地址**: `PUT /api/auth/password`

**接口描述**: 修改当前用户密码

**请求参数**:
```json
{
  "oldPassword": "123456",
  "newPassword": "newpass123",
  "confirmPassword": "newpass123"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| oldPassword | String | 是 | 旧密码 |
| newPassword | String | 是 | 新密码 |
| confirmPassword | String | 是 | 确认新密码 |

**响应示例**:
```json
{
  "code": 200,
  "message": "密码修改成功",
  "data": null
}
```

---

### 2.5 退出登录

**接口地址**: `POST /api/auth/logout`

**接口描述**: 用户退出登录，清除token

**请求参数**: 无

**响应示例**:
```json
{
  "code": 200,
  "message": "退出成功",
  "data": null
}
```

---

## 3. 用户管理模块 API

### 3.1 获取用户列表

**接口地址**: `GET /api/users`

**接口描述**: 获取所有用户列表（需要管理员权限）

**请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| pageNum | Integer | 否 | 页码，默认1 |
| pageSize | Integer | 否 | 每页数量，默认20 |
| keyword | String | 否 | 搜索关键词（用户名/手机号） |
| role | String | 否 | 角色筛选：admin/user |
| status | String | 否 | 状态筛选：active/inactive |

**响应示例**:
```json
{
  "code": 200,
  "message": "成功",
  "data": {
    "list": [
      {
        "id": "1",
        "userId": "user123",
        "username": "admin",
        "phone": "13800138000",
        "role": "admin",
        "status": "active",
        "createdAt": "2025-09-22T10:00:00Z"
      }
    ],
    "pageNum": 1,
    "pageSize": 20,
    "total": 50
  }
}
```

---

### 3.2 获取用户详情

**接口地址**: `GET /api/users/{id}`

**接口描述**: 获取指定用户的详细信息

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | String | 是 | 用户ID |

**响应示例**:
```json
{
  "code": 200,
  "message": "成功",
  "data": {
    "id": "1",
    "userId": "user123",
    "username": "admin",
    "phone": "13800138000",
    "role": "admin",
    "status": "active",
    "createdAt": "2025-09-22T10:00:00Z",
    "updatedAt": "2025-09-27T15:30:00Z"
  }
}
```

---

### 3.3 更新用户角色

**接口地址**: `PUT /api/users/{id}/role`

**接口描述**: 修改用户角色（需要管理员权限）

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | String | 是 | 用户ID |

**请求参数**:
```json
{
  "role": "admin"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| role | String | 是 | 角色：admin/user |

**响应示例**:
```json
{
  "code": 200,
  "message": "角色更新成功",
  "data": null
}
```

---

### 3.4 更新用户状态

**接口地址**: `PUT /api/users/{id}/status`

**接口描述**: 启用或禁用用户账户（需要管理员权限）

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | String | 是 | 用户ID |

**请求参数**:
```json
{
  "status": "inactive"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| status | String | 是 | 状态：active/inactive |

**响应示例**:
```json
{
  "code": 200,
  "message": "状态更新成功",
  "data": null
}
```

---

### 3.5 删除用户

**接口地址**: `DELETE /api/users/{id}`

**接口描述**: 删除指定用户（需要管理员权限）

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | String | 是 | 用户ID |

**响应示例**:
```json
{
  "code": 200,
  "message": "用户删除成功",
  "data": null
}
```

---

## 4. 美食分类管理模块 API

### 4.1 获取分类列表

**接口地址**: `GET /api/categories`

**接口描述**: 获取所有美食分类，按排序字段升序排列

**请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| keyword | String | 否 | 搜索关键词 |

**响应示例**:
```json
{
  "code": 200,
  "message": "成功",
  "data": [
    {
      "id": "1",
      "name": "主食",
      "description": "米饭、面条等主食类",
      "sortOrder": 1,
      "createdAt": "2025-09-22T10:00:00Z"
    },
    {
      "id": "2",
      "name": "菜肴",
      "description": "各类菜肴",
      "sortOrder": 2,
      "createdAt": "2025-09-22T10:00:00Z"
    }
  ]
}
```

---

### 4.2 创建分类

**接口地址**: `POST /api/categories`

**接口描述**: 创建新的美食分类（需要管理员权限）

**请求参数**:
```json
{
  "name": "汤羹",
  "description": "各类汤品",
  "sortOrder": 3
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| name | String | 是 | 分类名称（2-50字符） |
| description | String | 否 | 分类描述 |
| sortOrder | Integer | 否 | 排序值，默认0 |

**响应示例**:
```json
{
  "code": 200,
  "message": "分类创建成功",
  "data": {
    "id": "3",
    "name": "汤羹",
    "description": "各类汤品",
    "sortOrder": 3,
    "createdAt": "2025-10-03T14:20:00Z"
  }
}
```

---

### 4.3 更新分类

**接口地址**: `PUT /api/categories/{id}`

**接口描述**: 更新指定分类信息（需要管理员权限）

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | String | 是 | 分类ID |

**请求参数**:
```json
{
  "name": "汤羹类",
  "description": "各类汤品和羹类",
  "sortOrder": 3
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "分类更新成功",
  "data": null
}
```

---

### 4.4 删除分类

**接口地址**: `DELETE /api/categories/{id}`

**接口描述**: 删除指定分类（需要管理员权限）

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | String | 是 | 分类ID |

**响应示例**:
```json
{
  "code": 200,
  "message": "分类删除成功",
  "data": null
}
```

---

## 5. 美食标签管理模块 API

### 5.1 获取标签列表

**接口地址**: `GET /api/tags`

**接口描述**: 获取所有美食标签

**请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| keyword | String | 否 | 搜索关键词 |

**响应示例**:
```json
{
  "code": 200,
  "message": "成功",
  "data": [
    {
      "id": "1",
      "name": "正宗",
      "color": "#ff6b35",
      "createdAt": "2025-09-22T10:00:00Z"
    },
    {
      "id": "2",
      "name": "好吃",
      "color": "#f7931e",
      "createdAt": "2025-09-22T10:00:00Z"
    }
  ]
}
```

---

### 5.2 创建标签

**接口地址**: `POST /api/tags`

**接口描述**: 创建新的美食标签（需要管理员权限）

**请求参数**:
```json
{
  "name": "实惠",
  "color": "#3b82f6"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| name | String | 是 | 标签名称（2-20字符） |
| color | String | 否 | 标签颜色（HEX格式），默认#3b82f6 |

**响应示例**:
```json
{
  "code": 200,
  "message": "标签创建成功",
  "data": {
    "id": "3",
    "name": "实惠",
    "color": "#3b82f6",
    "createdAt": "2025-10-03T14:25:00Z"
  }
}
```

---

### 5.3 更新标签

**接口地址**: `PUT /api/tags/{id}`

**接口描述**: 更新指定标签信息（需要管理员权限）

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | String | 是 | 标签ID |

**请求参数**:
```json
{
  "name": "超实惠",
  "color": "#10b981"
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "标签更新成功",
  "data": null
}
```

---

### 5.4 删除标签

**接口地址**: `DELETE /api/tags/{id}`

**接口描述**: 删除指定标签（需要管理员权限）

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | String | 是 | 标签ID |

**响应示例**:
```json
{
  "code": 200,
  "message": "标签删除成功",
  "data": null
}
```

---

## 6. 美食管理模块 API

### 6.1 获取美食列表

**接口地址**: `GET /api/foods`

**接口描述**: 获取所有美食信息，包含分类、标签和评论数

**请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| pageNum | Integer | 否 | 页码，默认1 |
| pageSize | Integer | 否 | 每页数量，默认20 |
| keyword | String | 否 | 搜索关键词（名称/描述） |
| categoryId | String | 否 | 分类ID筛选 |
| status | String | 否 | 状态筛选：active/inactive |
| tagIds | String | 否 | 标签ID列表，逗号分隔 |

**响应示例**:
```json
{
  "code": 200,
  "message": "成功",
  "data": {
    "list": [
      {
        "id": "1",
        "name": "红烧肉",
        "description": "经典红烧肉，肥而不腻",
        "categoryId": "2",
        "category": {
          "id": "2",
          "name": "菜肴"
        },
        "imageUrl": "https://example.com/image.jpg",
        "price": 38.00,
        "rating": 4.5,
        "ratingCount": 128,
        "status": "active",
        "tags": [
          {
            "id": "1",
            "name": "正宗",
            "color": "#ff6b35"
          },
          {
            "id": "2",
            "name": "好吃",
            "color": "#f7931e"
          }
        ],
        "commentsCount": 45,
        "createdBy": "admin",
        "createdAt": "2025-09-22T10:00:00Z",
        "updatedAt": "2025-09-27T15:30:00Z"
      }
    ],
    "pageNum": 1,
    "pageSize": 20,
    "total": 150
  }
}
```

---

### 6.2 获取美食详情

**接口地址**: `GET /api/foods/{id}`

**接口描述**: 获取指定美食的详细信息

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | String | 是 | 美食ID |

**响应示例**:
```json
{
  "code": 200,
  "message": "成功",
  "data": {
    "id": "1",
    "name": "红烧肉",
    "description": "经典红烧肉，肥而不腻，色泽红亮，口感醇厚",
    "categoryId": "2",
    "category": {
      "id": "2",
      "name": "菜肴",
      "description": "各类菜肴"
    },
    "imageUrl": "https://example.com/image.jpg",
    "price": 38.00,
    "rating": 4.5,
    "ratingCount": 128,
    "status": "active",
    "tags": [
      {
        "id": "1",
        "name": "正宗",
        "color": "#ff6b35"
      }
    ],
    "commentsCount": 45,
    "createdBy": "admin",
    "createdAt": "2025-09-22T10:00:00Z",
    "updatedAt": "2025-09-27T15:30:00Z"
  }
}
```

---

### 6.3 创建美食

**接口地址**: `POST /api/foods`

**接口描述**: 创建新的美食条目（需要管理员权限）

**请求参数**:
```json
{
  "name": "糖醋排骨",
  "description": "酸甜可口的糖醋排骨",
  "categoryId": "2",
  "imageUrl": "https://example.com/ribs.jpg",
  "price": 45.00,
  "tagIds": ["1", "2"]
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| name | String | 是 | 美食名称（2-100字符） |
| description | String | 否 | 美食描述 |
| categoryId | String | 否 | 分类ID |
| imageUrl | String | 否 | 图片URL |
| price | BigDecimal | 否 | 价格 |
| tagIds | Array | 否 | 标签ID数组 |

**响应示例**:
```json
{
  "code": 200,
  "message": "美食创建成功",
  "data": {
    "id": "15",
    "name": "糖醋排骨",
    "description": "酸甜可口的糖醋排骨",
    "categoryId": "2",
    "imageUrl": "https://example.com/ribs.jpg",
    "price": 45.00,
    "rating": 0.0,
    "ratingCount": 0,
    "status": "active",
    "createdAt": "2025-10-03T14:30:00Z"
  }
}
```

---

### 6.4 更新美食

**接口地址**: `PUT /api/foods/{id}`

**接口描述**: 更新指定美食信息（需要管理员权限）

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | String | 是 | 美食ID |

**请求参数**:
```json
{
  "name": "糖醋排骨（改良版）",
  "description": "改良版糖醋排骨，更加酸甜可口",
  "categoryId": "2",
  "imageUrl": "https://example.com/ribs-new.jpg",
  "price": 48.00,
  "tagIds": ["1", "2", "3"]
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "美食更新成功",
  "data": null
}
```

---

### 6.5 删除美食

**接口地址**: `DELETE /api/foods/{id}`

**接口描述**: 删除指定美食（需要管理员权限）

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | String | 是 | 美食ID |

**响应示例**:
```json
{
  "code": 200,
  "message": "美食删除成功",
  "data": null
}
```

---

### 6.6 更新美食状态

**接口地址**: `PUT /api/foods/{id}/status`

**接口描述**: 启用或禁用美食条目（需要管理员权限）

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | String | 是 | 美食ID |

**请求参数**:
```json
{
  "status": "inactive"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| status | String | 是 | 状态：active/inactive |

**响应示例**:
```json
{
  "code": 200,
  "message": "状态更新成功",
  "data": null
}
```

---

## 7. 评论管理模块 API

### 7.1 获取评论列表

**接口地址**: `GET /api/comments`

**接口描述**: 获取所有评论，包含美食和用户信息

**请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| pageNum | Integer | 否 | 页码，默认1 |
| pageSize | Integer | 否 | 每页数量，默认20 |
| keyword | String | 否 | 搜索关键词（评论内容） |
| foodId | String | 否 | 美食ID筛选 |
| userId | String | 否 | 用户ID筛选 |
| status | String | 否 | 状态筛选：pending/approved/rejected |
| rating | Integer | 否 | 评分筛选：1-5 |

**响应示例**:
```json
{
  "code": 200,
  "message": "成功",
  "data": {
    "list": [
      {
        "id": "1",
        "foodId": "1",
        "food": {
          "id": "1",
          "name": "红烧肉"
        },
        "userId": "2",
        "userPhone": "13800138001",
        "content": "味道很好，肥而不腻，推荐！",
        "rating": 5,
        "status": "approved",
        "createdAt": "2025-09-25T16:20:00Z"
      }
    ],
    "pageNum": 1,
    "pageSize": 20,
    "total": 320
  }
}
```

---

### 7.2 获取评论详情

**接口地址**: `GET /api/comments/{id}`

**接口描述**: 获取指定评论的详细信息

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | String | 是 | 评论ID |

**响应示例**:
```json
{
  "code": 200,
  "message": "成功",
  "data": {
    "id": "1",
    "foodId": "1",
    "food": {
      "id": "1",
      "name": "红烧肉",
      "imageUrl": "https://example.com/image.jpg"
    },
    "userId": "2",
    "userPhone": "13800138001",
    "content": "味道很好，肥而不腻，推荐！",
    "rating": 5,
    "status": "approved",
    "createdAt": "2025-09-25T16:20:00Z"
  }
}
```

---

### 7.3 创建评论（C端用户使用）

**接口地址**: `POST /api/comments`

**接口描述**: 用户对美食发表评论和评分

**请求参数**:
```json
{
  "foodId": "1",
  "content": "非常美味，强烈推荐！",
  "rating": 5
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| foodId | String | 是 | 美食ID |
| content | String | 是 | 评论内容（5-500字符） |
| rating | Integer | 是 | 评分（1-5） |

**响应示例**:
```json
{
  "code": 200,
  "message": "评论发表成功，等待审核",
  "data": {
    "id": "35",
    "foodId": "1",
    "userId": "2",
    "content": "非常美味，强烈推荐！",
    "rating": 5,
    "status": "pending",
    "createdAt": "2025-10-03T14:35:00Z"
  }
}
```

---

### 7.4 更新评论状态

**接口地址**: `PUT /api/comments/{id}/status`

**接口描述**: 审核评论（需要管理员权限）

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | String | 是 | 评论ID |

**请求参数**:
```json
{
  "status": "approved"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| status | String | 是 | 状态：pending/approved/rejected |

**响应示例**:
```json
{
  "code": 200,
  "message": "评论审核成功",
  "data": null
}
```

---

### 7.5 删除评论

**接口地址**: `DELETE /api/comments/{id}`

**接口描述**: 删除指定评论（需要管理员权限）

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | String | 是 | 评论ID |

**响应示例**:
```json
{
  "code": 200,
  "message": "评论删除成功",
  "data": null
}
```

---

### 7.6 批量审核评论

**接口地址**: `PUT /api/comments/batch-status`

**接口描述**: 批量审核多条评论（需要管理员权限）

**请求参数**:
```json
{
  "ids": ["1", "2", "3"],
  "status": "approved"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| ids | Array | 是 | 评论ID数组 |
| status | String | 是 | 状态：approved/rejected |

**响应示例**:
```json
{
  "code": 200,
  "message": "批量审核成功",
  "data": {
    "successCount": 3,
    "failCount": 0
  }
}
```

---

## 8. 榜单管理模块 API

### 8.1 获取榜单列表

**接口地址**: `GET /api/rankings`

**接口描述**: 获取所有榜单，包含关联的美食信息

**请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| pageNum | Integer | 否 | 页码，默认1 |
| pageSize | Integer | 否 | 每页数量，默认20 |
| keyword | String | 否 | 搜索关键词（标题） |
| type | String | 否 | 榜单类型筛选 |
| status | String | 否 | 状态筛选：active/inactive |

**响应示例**:
```json
{
  "code": 200,
  "message": "成功",
  "data": {
    "list": [
      {
        "id": "1",
        "title": "本周热门美食榜",
        "description": "本周最受欢迎的美食排行",
        "type": "weekly",
        "status": "active",
        "foods": [
          {
            "id": "rf1",
            "rankPosition": 1,
            "food": {
              "id": "1",
              "name": "红烧肉",
              "rating": 4.8,
              "imageUrl": "https://example.com/image.jpg"
            }
          },
          {
            "id": "rf2",
            "rankPosition": 2,
            "food": {
              "id": "5",
              "name": "糖醋排骨",
              "rating": 4.7,
              "imageUrl": "https://example.com/ribs.jpg"
            }
          }
        ],
        "createdBy": "admin",
        "createdAt": "2025-09-28T10:00:00Z"
      }
    ],
    "pageNum": 1,
    "pageSize": 20,
    "total": 15
  }
}
```

---

### 8.2 获取榜单详情

**接口地址**: `GET /api/rankings/{id}`

**接口描述**: 获取指定榜单的详细信息

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | String | 是 | 榜单ID |

**响应示例**:
```json
{
  "code": 200,
  "message": "成功",
  "data": {
    "id": "1",
    "title": "本周热门美食榜",
    "description": "本周最受欢迎的美食排行",
    "type": "weekly",
    "status": "active",
    "foods": [
      {
        "id": "rf1",
        "rankPosition": 1,
        "food": {
          "id": "1",
          "name": "红烧肉",
          "description": "经典红烧肉",
          "rating": 4.8,
          "ratingCount": 128,
          "price": 38.00,
          "imageUrl": "https://example.com/image.jpg"
        }
      }
    ],
    "createdBy": "admin",
    "createdAt": "2025-09-28T10:00:00Z",
    "updatedAt": "2025-09-28T10:00:00Z"
  }
}
```

---

### 8.3 创建榜单

**接口地址**: `POST /api/rankings`

**接口描述**: 创建新的榜单（需要管理员权限）

**请求参数**:
```json
{
  "title": "本月新品推荐榜",
  "description": "本月新上架的优质美食",
  "type": "monthly",
  "foodIds": ["10", "11", "12", "13", "14"]
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| title | String | 是 | 榜单标题（2-100字符） |
| description | String | 否 | 榜单描述 |
| type | String | 否 | 榜单类型，默认general |
| foodIds | Array | 否 | 美食ID数组（按排序） |

**响应示例**:
```json
{
  "code": 200,
  "message": "榜单创建成功",
  "data": {
    "id": "6",
    "title": "本月新品推荐榜",
    "description": "本月新上架的优质美食",
    "type": "monthly",
    "status": "active",
    "createdAt": "2025-10-03T14:40:00Z"
  }
}
```

---

### 8.4 更新榜单

**接口地址**: `PUT /api/rankings/{id}`

**接口描述**: 更新榜单信息（需要管理员权限）

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | String | 是 | 榜单ID |

**请求参数**:
```json
{
  "title": "本月新品推荐榜（更新）",
  "description": "本月新上架的优质美食推荐",
  "type": "monthly",
  "foodIds": ["10", "11", "15", "13", "14"]
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "榜单更新成功",
  "data": null
}
```

---

### 8.5 删除榜单

**接口地址**: `DELETE /api/rankings/{id}`

**接口描述**: 删除指定榜单（需要管理员权限）

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | String | 是 | 榜单ID |

**响应示例**:
```json
{
  "code": 200,
  "message": "榜单删除成功",
  "data": null
}
```

---

### 8.6 更新榜单状态

**接口地址**: `PUT /api/rankings/{id}/status`

**接口描述**: 启用或禁用榜单（需要管理员权限）

**路径参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | String | 是 | 榜单ID |

**请求参数**:
```json
{
  "status": "inactive"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| status | String | 是 | 状态：active/inactive |

**响应示例**:
```json
{
  "code": 200,
  "message": "状态更新成功",
  "data": null
}
```

---

## 9. 统计数据模块 API

### 9.1 获取仪表盘统计数据

**接口地址**: `GET /api/stats/dashboard`

**接口描述**: 获取系统总体统计数据

**请求参数**: 无

**响应示例**:
```json
{
  "code": 200,
  "message": "成功",
  "data": {
    "totalFoods": 150,
    "totalUsers": 2580,
    "totalComments": 3420,
    "pendingComments": 25,
    "totalRankings": 15,
    "avgRating": 4.3
  }
}
```

---

### 9.2 获取热门美食排行

**接口地址**: `GET /api/stats/popular-foods`

**接口描述**: 获取评分最高的美食列表

**请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| limit | Integer | 否 | 返回数量，默认10 |

**响应示例**:
```json
{
  "code": 200,
  "message": "成功",
  "data": [
    {
      "id": "1",
      "name": "红烧肉",
      "rating": 4.8,
      "ratingCount": 128,
      "categoryName": "菜肴"
    },
    {
      "id": "5",
      "name": "糖醋排骨",
      "rating": 4.7,
      "ratingCount": 95,
      "categoryName": "菜肴"
    }
  ]
}
```

---

### 9.3 获取用户活动统计

**接口地址**: `GET /api/stats/user-activity`

**接口描述**: 获取指定天数内的用户活动数据

**请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| days | Integer | 否 | 统计天数，默认7天 |

**响应示例**:
```json
{
  "code": 200,
  "message": "成功",
  "data": [
    {
      "date": "2025-10-03",
      "newUsers": 12,
      "newComments": 45,
      "newFoods": 3
    },
    {
      "date": "2025-10-02",
      "newUsers": 15,
      "newComments": 52,
      "newFoods": 2
    }
  ]
}
```

---

### 9.4 获取评分统计

**接口地址**: `GET /api/stats/rating-stats`

**接口描述**: 获取美食评分趋势和分布统计

**请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| foodId | String | 否 | 美食ID（可选，不传则统计全部） |
| days | Integer | 否 | 统计天数，默认30天 |

**响应示例**:
```json
{
  "code": 200,
  "message": "成功",
  "data": {
    "avgRating": 4.3,
    "totalRatings": 3420,
    "ratingDistribution": {
      "5": 1520,
      "4": 1280,
      "3": 480,
      "2": 105,
      "1": 35
    },
    "trendData": [
      {
        "date": "2025-10-03",
        "avgRating": 4.4,
        "count": 45
      },
      {
        "date": "2025-10-02",
        "avgRating": 4.3,
        "count": 52
      }
    ]
  }
}
```

---

### 9.5 获取访问统计

**接口地址**: `GET /api/stats/visit-stats`

**接口描述**: 获取用户访问和行为统计数据

**请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| days | Integer | 否 | 统计天数，默认30天 |
| type | String | 否 | 统计类型：pv/uv/all，默认all |

**响应示例**:
```json
{
  "code": 200,
  "message": "成功",
  "data": {
    "totalPV": 125800,
    "totalUV": 35600,
    "avgVisitDuration": 325,
    "bounceRate": 0.32,
    "dailyStats": [
      {
        "date": "2025-10-03",
        "pv": 4520,
        "uv": 1280
      },
      {
        "date": "2025-10-02",
        "pv": 4680,
        "uv": 1320
      }
    ],
    "topPages": [
      {
        "page": "美食详情",
        "pv": 45200,
        "uv": 12800
      },
      {
        "page": "美食列表",
        "pv": 38500,
        "uv": 11200
      }
    ]
  }
}
```

---

### 9.6 获取分类统计

**接口地址**: `GET /api/stats/category-stats`

**接口描述**: 获取各分类的美食数量和热度统计

**请求参数**: 无

**响应示例**:
```json
{
  "code": 200,
  "message": "成功",
  "data": [
    {
      "categoryId": "1",
      "categoryName": "主食",
      "foodCount": 45,
      "totalComments": 520,
      "avgRating": 4.2
    },
    {
      "categoryId": "2",
      "categoryName": "菜肴",
      "foodCount": 68,
      "totalComments": 1850,
      "avgRating": 4.5
    }
  ]
}
```

---

## 10. 数据库表结构

### 10.1 用户表 (profiles)

```sql
CREATE TABLE profiles (
    id VARCHAR(50) PRIMARY KEY COMMENT '主键ID',
    user_id VARCHAR(50) UNIQUE NOT NULL COMMENT '用户唯一标识',
    username VARCHAR(50) UNIQUE NOT NULL COMMENT '用户名',
    password_hash VARCHAR(255) COMMENT '密码哈希',
    phone VARCHAR(20) COMMENT '手机号',
    role ENUM('admin', 'user') DEFAULT 'user' COMMENT '角色',
    status ENUM('active', 'inactive') DEFAULT 'active' COMMENT '状态',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_username (username),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

---

### 10.2 美食分类表 (food_categories)

```sql
CREATE TABLE food_categories (
    id VARCHAR(50) PRIMARY KEY COMMENT '主键ID',
    name VARCHAR(50) NOT NULL COMMENT '分类名称',
    description TEXT COMMENT '分类描述',
    sort_order INT DEFAULT 0 COMMENT '排序',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    INDEX idx_sort (sort_order)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='美食分类表';
```

---

### 10.3 美食标签表 (food_tags)

```sql
CREATE TABLE food_tags (
    id VARCHAR(50) PRIMARY KEY COMMENT '主键ID',
    name VARCHAR(20) NOT NULL COMMENT '标签名称',
    color VARCHAR(20) DEFAULT '#3b82f6' COMMENT '标签颜色',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    INDEX idx_name (name)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='美食标签表';
```

---

### 10.4 美食表 (foods)

```sql
CREATE TABLE foods (
    id VARCHAR(50) PRIMARY KEY COMMENT '主键ID',
    name VARCHAR(100) NOT NULL COMMENT '美食名称',
    description TEXT COMMENT '美食描述',
    category_id VARCHAR(50) COMMENT '分类ID',
    image_url VARCHAR(255) COMMENT '图片URL',
    price DECIMAL(10,2) COMMENT '价格',
    rating DECIMAL(3,1) DEFAULT 0.0 COMMENT '评分',
    rating_count INT DEFAULT 0 COMMENT '评价数',
    status ENUM('active', 'inactive') DEFAULT 'active' COMMENT '状态',
    created_by VARCHAR(50) COMMENT '创建人',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_category (category_id),
    INDEX idx_rating (rating),
    INDEX idx_status (status),
    INDEX idx_name (name),
    FOREIGN KEY (category_id) REFERENCES food_categories(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='美食表';
```

---

### 10.5 美食标签关联表 (food_tag_relations)

```sql
CREATE TABLE food_tag_relations (
    id VARCHAR(50) PRIMARY KEY COMMENT '主键ID',
    food_id VARCHAR(50) NOT NULL COMMENT '美食ID',
    tag_id VARCHAR(50) NOT NULL COMMENT '标签ID',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    UNIQUE KEY uk_food_tag (food_id, tag_id),
    INDEX idx_food (food_id),
    INDEX idx_tag (tag_id),
    FOREIGN KEY (food_id) REFERENCES foods(id) ON DELETE CASCADE,
    FOREIGN KEY (tag_id) REFERENCES food_tags(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='美食标签关联表';
```

---

### 10.6 评论表 (comments)

```sql
CREATE TABLE comments (
    id VARCHAR(50) PRIMARY KEY COMMENT '主键ID',
    food_id VARCHAR(50) NOT NULL COMMENT '美食ID',
    user_id VARCHAR(50) NOT NULL COMMENT '用户ID',
    content TEXT NOT NULL COMMENT '评论内容',
    rating INT NOT NULL COMMENT '评分(1-5)',
    status ENUM('pending', 'approved', 'rejected') DEFAULT 'pending' COMMENT '审核状态',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    INDEX idx_food (food_id),
    INDEX idx_user (user_id),
    INDEX idx_status (status),
    INDEX idx_rating (rating),
    FOREIGN KEY (food_id) REFERENCES foods(id) ON DELETE CASCADE,
    FOREIGN KEY (user_id) REFERENCES profiles(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='评论表';
```

---

### 10.7 榜单表 (rankings)

```sql
CREATE TABLE rankings (
    id VARCHAR(50) PRIMARY KEY COMMENT '主键ID',
    title VARCHAR(100) NOT NULL COMMENT '榜单标题',
    description TEXT COMMENT '榜单描述',
    type VARCHAR(50) DEFAULT 'general' COMMENT '榜单类型',
    status ENUM('active', 'inactive') DEFAULT 'active' COMMENT '状态',
    created_by VARCHAR(50) COMMENT '创建人',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_type (type),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='榜单表';
```

---

### 10.8 榜单美食关联表 (ranking_foods)

```sql
CREATE TABLE ranking_foods (
    id VARCHAR(50) PRIMARY KEY COMMENT '主键ID',
    ranking_id VARCHAR(50) NOT NULL COMMENT '榜单ID',
    food_id VARCHAR(50) NOT NULL COMMENT '美食ID',
    rank_position INT NOT NULL COMMENT '排名位置',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    UNIQUE KEY uk_ranking_food (ranking_id, food_id),
    INDEX idx_ranking (ranking_id),
    INDEX idx_position (rank_position),
    FOREIGN KEY (ranking_id) REFERENCES rankings(id) ON DELETE CASCADE,
    FOREIGN KEY (food_id) REFERENCES foods(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='榜单美食关联表';
```

---

## 11. 常量定义

### 11.1 角色常量
```java
public class RoleConstants {
    public static final String ADMIN = "admin";      // 管理员
    public static final String USER = "user";        // 普通用户
}
```

### 11.2 状态常量
```java
public class StatusConstants {
    public static final String ACTIVE = "active";       // 启用
    public static final String INACTIVE = "inactive";   // 禁用
}
```

### 11.3 评论状态常量
```java
public class CommentStatusConstants {
    public static final String PENDING = "pending";      // 待审核
    public static final String APPROVED = "approved";    // 已通过
    public static final String REJECTED = "rejected";    // 已拒绝
}
```

### 11.4 响应码常量
```java
public class ResponseCode {
    public static final int SUCCESS = 200;              // 成功
    public static final int BAD_REQUEST = 400;          // 请求参数错误
    public static final int UNAUTHORIZED = 401;         // 未授权
    public static final int FORBIDDEN = 403;            // 无权限
    public static final int NOT_FOUND = 404;            // 资源不存在
    public static final int INTERNAL_ERROR = 500;       // 服务器错误
}
```

---

## 12. 异常处理

### 12.1 业务异常
```java
public class BusinessException extends RuntimeException {
    private int code;
    private String message;
    
    public BusinessException(int code, String message) {
        super(message);
        this.code = code;
        this.message = message;
    }
}
```

### 12.2 全局异常处理器
```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    
    @ExceptionHandler(BusinessException.class)
    public Result handleBusinessException(BusinessException e) {
        return Result.error(e.getCode(), e.getMessage());
    }
    
    @ExceptionHandler(Exception.class)
    public Result handleException(Exception e) {
        log.error("系统异常", e);
        return Result.error(500, "系统错误，请稍后重试");
    }
}
```

---

## 13. 安全相关

### 13.1 密码加密
- 使用BCrypt算法进行密码加密
- 加密强度：12 rounds

### 13.2 JWT Token
- Token过期时间：24小时
- 刷新Token过期时间：7天
- Token存储位置：请求头 Authorization: Bearer {token}

### 13.3 权限控制
- 使用Spring Security + JWT进行认证和授权
- 接口级别的权限控制使用@PreAuthorize注解
- 管理员权限：@PreAuthorize("hasRole('ADMIN')")

### 13.4 接口限流
- 使用Redis实现接口限流
- 限流规则：单IP每秒最多100次请求
- 超限返回：429 Too Many Requests

---

## 14. 开发规范

### 14.1 命名规范
- Controller层：XxxController
- Service层：XxxService / XxxServiceImpl
- Mapper层：XxxMapper
- Entity层：Xxx
- DTO层：XxxDTO
- VO层：XxxVO

### 14.2 接口规范
- RESTful API设计风格
- URL使用小写字母，单词间使用"-"分隔
- GET：查询操作
- POST：创建操作
- PUT：更新操作
- DELETE：删除操作

### 14.3 日志规范
- 使用SLF4J + Logback
- 日志级别：ERROR > WARN > INFO > DEBUG > TRACE
- 关键操作必须记录日志

---

## 15. 测试说明

### 15.1 测试账户

#### 管理员账户
- 用户名：admin
- 密码：admin123
- 权限：完全访问权限

#### 普通用户账户
- 用户名：testuser
- 密码：123456
- 权限：基本浏览权限

### 15.2 接口测试工具
- Postman
- Swagger UI (开发环境可访问 /swagger-ui.html)

---

## 16. 部署说明

### 16.1 环境要求
- JDK 8+
- MySQL 5.7+
- Redis 5.0+
- Maven 3.6+

### 16.2 配置文件
```yaml
# application.yml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cateen?useUnicode=true&characterEncoding=utf8
    username: root
    password: 123456
  redis:
    host: localhost
    port: 6379
    
server:
  port: 8080
  
jwt:
  secret: your-secret-key
  expiration: 86400000  # 24小时
```

---

## 17. 更新日志

### v1.0 (2025-10-03)
- ✅ 完成认证模块接口设计
- ✅ 完成用户管理模块接口设计
- ✅ 完成美食分类管理模块接口设计
- ✅ 完成美食标签管理模块接口设计
- ✅ 完成美食管理模块接口设计
- ✅ 完成评论管理模块接口设计
- ✅ 完成榜单管理模块接口设计
- ✅ 完成统计数据模块接口设计
- ✅ 完成数据库表结构设计

---

## 18. 联系方式

如有问题或建议，请联系开发团队。

---

**文档版本**: v1.0  
**最后更新**: 2025-10-03  
**文档状态**: 已完成

